<h1>Listing package_categories</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @package_categories.each do |package_category| %>
  <tr>
    <td><%= package_category.name %></td>
    <td><%= package_category.description %></td>
    <td><%= link_to 'Show', package_category %></td>
    <td><%= link_to 'Edit', edit_package_category_path(package_category) %></td>
    <td><%= link_to 'Destroy', package_category, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Package category', new_package_category_path %>
