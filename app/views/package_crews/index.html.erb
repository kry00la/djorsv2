<h1>Listing package_crews</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @package_crews.each do |package_crew| %>
  <tr>
    <td><%= link_to 'Show', package_crew %></td>
    <td><%= link_to 'Edit', edit_package_crew_path(package_crew) %></td>
    <td><%= link_to 'Destroy', package_crew, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Package crew', new_package_crew_path %>
