<h1>Listing package_menus</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @package_menus.each do |package_menu| %>
  <tr>
    <td><%= link_to 'Show', package_menu %></td>
    <td><%= link_to 'Edit', edit_package_menu_path(package_menu) %></td>
    <td><%= link_to 'Destroy', package_menu, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Package menu', new_package_menu_path %>
