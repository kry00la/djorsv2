<%= form_for(@content_intro_u) do |f| %>
  <% if @content_intro_u.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@content_intro_u.errors.count, "error") %> prohibited this content_intro_u from being saved:</h2>

      <ul>
      <% @content_intro_u.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
