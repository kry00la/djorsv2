<%= form_for(@image_slide) do |f| %>
  <% if @image_slide.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@image_slide.errors.count, "error") %> prohibited this image_slide from being saved:</h2>

      <ul>
      <% @image_slide.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
